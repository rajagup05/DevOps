
## creating temporary user account with an expiry date

`Task`: **Create a user named javed on App Server 2. Set the expiry date to 2026-12-07**

- used command `Sudo useradd javed -e 2026-12-07` to add a user named `javed`, here `-e` or `--expiredate` is determines expiry date.

